import fs from 'node:fs';
import { writeFile } from 'node:fs/promises';
import { pathToFileURL } from 'node:url';
import { Pool } from 'pg';
import { validateProductionTarget } from '../catalogue/production-target-check.mjs';
import { normaliseComparableName } from '../catalogue/reconcile.mjs';
import { fetchCardmarketPokemonSinglesCatalogue, fetchCardmarketPokemonPriceGuide } from './cardmarket-source-client.mjs';
import { hasMeaningfulCardmarketLane } from './cardmarket-adapter.mjs';
import { loadTcgdexRepositoryEvidence } from './tcgdex-repository-cardmarket-evidence.mjs';

const key=(...parts)=>parts.join('|');
const sourceVariantFor=Object.freeze({standard:'normal',holo:'holo'});
const laneFor=Object.freeze({standard:'standard',holo:'holo'});
const SP_MARKERS=new Set(['G','GL','FB','C','E4','M']);

function findBalancedEnd(source,start,openChar,closeChar){if(source[start]!==openChar)return -1;let depth=0,quote=null,escaped=false,lineComment=false,blockComment=false;for(let i=start;i<source.length;i++){const c=source[i],n=source[i+1];if(lineComment){if(c==='\n')lineComment=false;continue;}if(blockComment){if(c==='*'&&n==='/'){blockComment=false;i++;}continue;}if(quote){if(escaped){escaped=false;continue;}if(c==='\\'){escaped=true;continue;}if(c===quote)quote=null;continue;}if(c==='/'&&n==='/'){lineComment=true;i++;continue;}if(c==='/'&&n==='*'){blockComment=true;i++;continue;}if(c==='"'||c==="'"||c==='`'){quote=c;continue;}if(c===openChar)depth++;else if(c===closeChar){depth--;if(depth===0)return i;}}return -1;}
function balancedAfter(source,regex,openChar,closeChar){const m=regex.exec(source);if(!m)return null;const start=source.indexOf(openChar,m.index+m[0].length-1);if(start<0)return null;const end=findBalancedEnd(source,start,openChar,closeChar);return end<0?null:source.slice(start,end+1);}
function arrayProperty(source,property){return balancedAfter(source,new RegExp(`\\b${property}\\s*:\\s*\\[`),'[',']');}
function objectProperty(source,property){return balancedAfter(source,new RegExp(`\\b${property}\\s*:\\s*\\{`),'{','}');}
function quotedProperty(source,property){if(!source)return null;const m=new RegExp(`\\b${property}\\s*:\\s*(["'\\x60])([^\\n]*?)\\1`).exec(source);return m?m[2].trim():null;}
function topLevelObjects(arraySource){if(!arraySource||arraySource[0]!=='[')return[];const out=[];let i=1;while(i<arraySource.length-1){const open=arraySource.indexOf('{',i);if(open<0)break;const end=findBalancedEnd(arraySource,open,'{','}');if(end<0)break;out.push(arraySource.slice(open,end+1));i=end+1;}return out;}
function namesFromArray(source,property){const block=arrayProperty(source,property);if(!block)return[];return topLevelObjects(block).map(obj=>quotedProperty(objectProperty(obj,'name'),'en')).filter(Boolean);}
function descriptorEvidence(card){try{const source=fs.readFileSync(card.sourcePath,'utf8');return [...new Set([...namesFromArray(source,'attacks'),...namesFromArray(source,'abilities')].map(normaliseComparableName).filter(Boolean))].sort();}catch{return[];}}
function parseProvider(name){let value=String(name||'').trim().replace(/^Nidoran\s+\[F\](?=\s|$)/i,'Nidoran female').replace(/^Nidoran\s+\[M\](?=\s|$)/i,'Nidoran male');const groups=[];const suffix=/\s+\[([^\[\]]+)\]\s*$/;while(true){const m=suffix.exec(value);if(!m)break;groups.unshift(m[1]);value=value.slice(0,m.index).trim();}
  value=value.replace(/\s+Lv\.\s*\d+\b/gi,' ').replace(/δ\s+Delta Species\b/gi,'δ');
  value=value.replace(/\[([A-Za-z0-9]+)\]/g,(all,marker)=>SP_MARKERS.has(String(marker).toUpperCase())?` ${String(marker).toUpperCase()} `:all).replace(/\s+/g,' ').trim();
  const terms=[];for(const group of groups){for(const part of group.split('|')){const t=normaliseComparableName(part);if(t&&!/^\d+[a-z]?$/.test(t))terms.push(t);}}
  return {base:normaliseComparableName(value),terms:[...new Set(terms)].sort(),rawBase:value,groups};
}
function sameSet(a,b){return a.length>0&&a.length===b.length&&a.every((v,i)=>v===b[i]);}

async function main(){validateProductionTarget(process.env.DATABASE_URL);const pool=new Pool({connectionString:process.env.DATABASE_URL,max:2});const db=await pool.connect();let report;try{
  const repo=loadTcgdexRepositoryEvidence(process.env.TCGDEX_REPO,{includeCards:true});const cardById=new Map();for(const set of repo.sets)for(const card of set.cards)cardById.set(card.tcgdexCardId,card);
  const [{products},{snapshot}]=await Promise.all([fetchCardmarketPokemonSinglesCatalogue(),fetchCardmarketPokemonPriceGuide()]);const priceBy=new Map(snapshot.priceGuides.map(r=>[String(r.idProduct),r]));
  const {rows:sets}=await db.query(`SELECT s.id set_id,s.name set_name,t.source_record_id tcgdex_set_id,cm.source_record_id cm_override FROM fatedrop_card_sets s JOIN fatedrop_card_set_source_mappings t ON t.set_id=s.id AND t.source_name='tcgdex' LEFT JOIN fatedrop_card_set_source_mappings cm ON cm.set_id=s.id AND cm.source_name='cardmarket' WHERE s.verification_status='verified'`);const expansionBySet=new Map();for(const s of sets){const ev=repo.bySetId.get(s.tcgdex_set_id),o=Number(s.cm_override),e=Number(ev?.cardmarketExpansionId),x=Number.isSafeInteger(o)&&o>0?o:e;if(Number.isSafeInteger(x)&&x>0)expansionBySet.set(s.set_id,{id:x,name:s.set_name});}
  const {rows:ids}=await db.query(`SELECT DISTINCT ON(i.id) i.id,i.set_id,i.variant_code,p.name,p.collector_number,t.source_record_id tcgdex_card_id FROM fatedrop_card_identities i JOIN fatedrop_card_printings p ON p.id=i.printing_id JOIN fatedrop_card_source_mappings t ON t.card_identity_id=i.id AND t.source_name='tcgdex' WHERE i.verification_status='verified' AND i.language_code='en' AND i.variant_code IN('standard','holo') AND NOT EXISTS(SELECT 1 FROM fatedrop_card_source_mappings m WHERE m.source_name='cardmarket' AND m.card_identity_id=i.id) ORDER BY i.id,t.source_record_id`);
  const byExpansion=new Map();for(const p of products){const e=Number(p.sourceExpansionId);if(!Number.isSafeInteger(e)||e<=0)continue;const parsed=parseProvider(p.name);if(!parsed.base)continue;const k=key(e,parsed.base);const a=byExpansion.get(k)||[];a.push({p,parsed});byExpansion.set(k,a);}
  const {rows:existing}=await db.query(`SELECT card_identity_id,source_record_id,source_variant_key FROM fatedrop_card_source_mappings WHERE source_name='cardmarket'`);const sourceOwners=new Map(existing.map(r=>[key(r.source_record_id,r.source_variant_key),r.card_identity_id]));
  const counts={eligible:ids.length,withExpansion:0,withDescriptorEvidence:0,baseMatches:0,exactDescriptorUnique:0,priceable:0,safe:0,ambiguous:0,conflicts:0};const reasons={},candidates=[],samples=[],bySet={};const reason=r=>reasons[r]=(reasons[r]||0)+1;
  for(const i of ids){const scope=expansionBySet.get(i.set_id);if(!scope){reason('no_explicit_expansion');continue;}counts.withExpansion++;const card=cardById.get(i.tcgdex_card_id);if(!card){reason('missing_card_evidence');continue;}const evidence=descriptorEvidence(card);if(!evidence.length){reason('no_descriptor_evidence');continue;}counts.withDescriptorEvidence++;const matches=byExpansion.get(key(scope.id,normaliseComparableName(i.name)))||[];if(!matches.length){reason('no_legacy_base_match');continue;}counts.baseMatches++;const exact=matches.filter(x=>sameSet(x.parsed.terms,evidence));if(exact.length!==1){if(exact.length>1){counts.ambiguous++;reason('multiple_exact_descriptor_matches');}else reason('no_exact_descriptor_match');continue;}counts.exactDescriptorUnique++;const x=exact[0],sourceRecordId=String(x.p.sourceRecordId),sv=sourceVariantFor[i.variant_code],lane=laneFor[i.variant_code],pr=priceBy.get(sourceRecordId);if(!pr||!hasMeaningfulCardmarketLane(pr,lane)){reason('no_meaningful_price_lane');continue;}counts.priceable++;const sk=key(sourceRecordId,sv);if(sourceOwners.has(sk)&&sourceOwners.get(sk)!==i.id){counts.conflicts++;reason('source_owned');continue;}counts.safe++;bySet[scope.name]=(bySet[scope.name]||0)+1;candidates.push({identityId:i.id,setName:scope.name,name:i.name,collectorNumber:i.collector_number,variantCode:i.variant_code,sourceRecordId,cardmarketName:x.p.name,evidence,providerTerms:x.parsed.terms});if(samples.length<40)samples.push(candidates[candidates.length-1]);}
  report={status:'audit_complete',productionWrites:false,counts,reasons,bySet,candidates,samples};
}catch(e){report={status:'blocked',productionWrites:false,error:e instanceof Error?e.message:String(e)};process.exitCode=1;}finally{db.release();await pool.end();await writeFile(`${process.env.RUNNER_TEMP||'.'}/cardmarket-legacy-provider-metadata-audit.json`,JSON.stringify(report,null,2));console.log(JSON.stringify({status:report.status,counts:report.counts,reasons:report.reasons,bySet:report.bySet,samples:report.samples},null,2));}}
if(process.argv[1]&&import.meta.url===pathToFileURL(process.argv[1]).href)await main();
