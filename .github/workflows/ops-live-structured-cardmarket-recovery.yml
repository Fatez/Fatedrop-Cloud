name: OPS Live Structured Cardmarket Recovery

on:
  push:
    branches:
      - feat/cardmarket-live-structured-recovery-v2-2026-09-11
    paths:
      - '.github/workflows/ops-live-structured-cardmarket-recovery.yml'
      - 'signal-engine/src/trader/value/cardmarket-live-structured-recovery-cli.mjs'
  workflow_dispatch:
    inputs:
      write:
        description: 'Persist only conflict-free exact structured normal+holo mappings'
        required: true
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  recover:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    defaults:
      run:
        working-directory: signal-engine
    steps:
      - uses: actions/checkout@v4
      - name: Check out pinned TCGdex evidence
        uses: actions/checkout@v4
        with:
          repository: tcgdex/cards-database
          ref: 5b6a2859f454972477a9953ffe5cb554d24c45e9
          path: _evidence/tcgdex-cards-database
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: signal-engine/package-lock.json
      - run: npm ci
      - name: Recover exact expansion + collector + name mappings
        env:
          DATABASE_URL: ${{ secrets.FATEPRICE_PRODUCTION_DATABASE_URL }}
          TCGDEX_REPO: ${{ github.workspace }}/_evidence/tcgdex-cards-database
          TCGDEX_REVISION: 5b6a2859f454972477a9953ffe5cb554d24c45e9
          MAPPING_WRITE: ${{ inputs.write || 'false' }}
        run: node src/trader/value/cardmarket-live-structured-recovery-cli.mjs
      - name: Upload recovery audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cardmarket-live-structured-recovery
          path: ${{ runner.temp }}/cardmarket-live-structured-recovery.json
          if-no-files-found: error
          retention-days: 14
