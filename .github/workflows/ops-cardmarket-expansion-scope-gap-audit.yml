name: OPS Cardmarket Expansion Scope Gap Audit

on:
  push:
    branches:
      - feat/cardmarket-expansion-scope-gap-audit-2026-09-11
    paths:
      - '.github/workflows/ops-cardmarket-expansion-scope-gap-audit.yml'
      - 'signal-engine/src/trader/value/cardmarket-expansion-scope-gap-audit-cli.mjs'

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: signal-engine
    steps:
      - uses: actions/checkout@v4
      - name: Check out pinned TCGdex evidence
        uses: actions/checkout@v4
        with:
          repository: tcgdex/cards-database
          ref: 5b6a2859f454972477a9953ffe5cb554d24c45e9
          path: _evidence/tcgdex-cards-database
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: signal-engine/package-lock.json
      - run: npm ci
      - name: Audit missing Cardmarket expansion scopes
        env:
          DATABASE_URL: ${{ secrets.FATEPRICE_PRODUCTION_DATABASE_URL }}
          TCGDEX_REPO: ${{ github.workspace }}/_evidence/tcgdex-cards-database
        run: node src/trader/value/cardmarket-expansion-scope-gap-audit-cli.mjs
      - name: Upload audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cardmarket-expansion-scope-gap-audit
          path: ${{ runner.temp }}/cardmarket-expansion-scope-gap-audit.json
          if-no-files-found: error
          retention-days: 14
