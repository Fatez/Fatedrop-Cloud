name: Cardmarket Residual Diagnostics

on:
  push:
    branches:
      - audit/cardmarket-residual-diagnostics-2026-09-12
    paths:
      - 'signal-engine/src/trader/value/cardmarket-residual-diagnostics-cli.mjs'
      - '.github/workflows/cardmarket-residual-diagnostics.yml'

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: signal-engine
    steps:
      - uses: actions/checkout@v4
      - name: Check out pinned TCGdex evidence
        uses: actions/checkout@v4
        with:
          repository: tcgdex/cards-database
          ref: 5b6a2859f454972477a9953ffe5cb554d24c45e9
          path: _evidence/tcgdex-cards-database
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: signal-engine/package-lock.json
      - run: npm ci
      - name: Run residual diagnostic
        env:
          DATABASE_URL: ${{ secrets.FATEPRICE_PRODUCTION_DATABASE_URL }}
          TCGDEX_REPO: ${{ github.workspace }}/_evidence/tcgdex-cards-database
          MAPPING_WRITE: 'false'
        run: node src/trader/value/cardmarket-residual-diagnostics-cli.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cardmarket-residual-diagnostics
          path: ${{ runner.temp }}/cardmarket-residual-diagnostics.json
          if-no-files-found: error
          retention-days: 14
